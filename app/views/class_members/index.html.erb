<div class="page-header">
  <h1>
    Class members
    <a href="/class_members/new" class="btn btn-lg btn-success">New Class member</a>
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Student</th>
        <th>Course</th>
        <th>Actions</th>
      </tr>

      <% @class_members.each do |class_member| %>
      <tr>
        <td><%= class_member.student.name %></td>
        <td><%= class_member.class.created_at %></td>
        <td>
          <a href="/class_members/<%= class_member.id %>" class="btn btn-primary">Show</a>
          <a href="/class_members/<%= class_member.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_class_member/<%= class_member.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
    <%= paginate @class_members, theme: 'twitter-bootstrap-4' %>
  </div>
</div>
